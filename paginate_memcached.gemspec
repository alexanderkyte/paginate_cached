Gem::Specification.new do |s|
  s.authors = ['Alexander Kyte']
  s.email = 'alexanderkyte@gmail.com'
  s.date = '2013-06-22'

  s.name = 'paginate_cached'
  s.version = '1.01'
  s.files = ['lib/paginate_cached.rb', 'init.rb', 'lib/paginate_cached/paginate_cached.rb', 'lib/paginate_cached/railtie.rb']
  s.homepage = 'http://github.com/alexanderkyte/paginate_cached'
  s.summary = 'Provides a helper method which will memoize result generation using the cache, and return the page requested.'
end
